#include <iostream>
using namespace std;

class Hash {
private:
    int a[20];  
    int n;        

public:
    Hash() {
        n = 20;
        for (int i = 0; i < n; i++)
            a[i] = -1;   
    }

    int h(int x) {
        return x % n;
    }

    void insert(int x) {
        int i = h(x);
        if (a[i] == -1) {
            a[i] = x;
            cout << "Inserted at index " << i << endl;
        } else {
            cout << "Collision! Cannot insert." << endl;
        }
    }

    void deleteKey(int x) {
        int i = h(x);
        if (a[i] == x) {
            a[i] = -1;
            cout << "Key deleted.\n";
        } else {
            cout << "Key not found.\n";
        }
    }

    void search(int x) {
        int i = h(x);
        if (a[i] == x)
            cout << "Key found at index " << i << endl;
        else
            cout << "Key not found.\n";
    }

  void display() {
    for (int i = 0; i < n; i++) {
        if (a[i] != -1)
            cout << i << ": " << a[i] << endl;
    }
}
};

int main() {
    Hash h;

    h.insert(1);
    h.insert(3);
    h.insert(4);
    h.insert(5);
    h.insert(7);
    h.display();
    h.deleteKey(4);
    h.display();
    h.search(5);
    h.search(6);
}
